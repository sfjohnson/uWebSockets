name: C++ CI

on: [push]

jobs:
  build_osx:
    runs-on: macos-10.15
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          persist-credentials: false

    - name: Build source
      run: make
