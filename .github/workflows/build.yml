name: Build

on: push

jobs:
  build:
    runs-on: macos-10.15
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          persist-credentials: false
          submodules: recursive

      - name: Build
        run: WITH_OPENSSL=1 make

      - name: List output
        run: ls .
